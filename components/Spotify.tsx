import React from "react";
import Link from "next/link";
import {CLIENT_ID, CLIENT_SECRET, REDIRECT_URI, AUTH_ENDPOINT, RESPONSE_TYPE} from "@/utils/spotifyKeys";

const Spotify = () => {
	return (
		// use the spotify api to get the top 5 songs of the past week from my account and display them in a nice way
		<div className="flex flex-col items-center justify-center">

		</div>
	);
};
